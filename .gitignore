# Python
__pycache__/
*.pyc
.venv/
venv/
.streamlit/

# Offline build cache (downloaded data, regenerated by build_proxy.py)
data/_proxy_cache/

# Community-submitted reports + photos - runtime state, not bundled source data.
# (Streamlit Cloud's disk is wiped on restart; use the in-app "Download all reports" button to back up.)
data/citizen_submissions.csv
data/citizen_photos/

# Tooling / local
.claude/
graphify-out/
.DS_Store

# IMPORTANT: the data CSVs are intentionally COMMITTED so the deployed app is
# self-contained and never downloads anything. Do NOT add `data/*.csv` here.
